{% extends 'admin/base.html.twig' %}

{% block body %}
{{parent()}}
    <div class="col-md-6 mx-auto ">
        
        <h1 class="py-5 text-center">Détails Utilisateur</h1>
        <a href="{{url('admin.user.list')}} " class="btn btn-light mb-2"> << Retour</a>
        <table class="table table-striped">
            <tbody>
                <tr>
                    <td>Nom: {{ user.username}} </td>
                </tr>
                <tr>
                    <td>Email: {{ user.email}} </td>
                </tr>
                <tr>
                    <td>Role: 
                        {% for role in user.roles %} 
                        {{ role }}
                        {% endfor %}
                    </td>
                </tr>
                
            </tbody>
        </table>
        
    </div>
     
{% endblock %}